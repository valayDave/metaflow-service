from pathlib import Path


BENCHMARK_FLOW_NAME = "AgenticBenchmarkFlow"
TARGET_ARTIFACT_NAME = "bench_target_artifact"
STDERR_ERROR_MARKER = "AGENTIC_BENCHMARK_ERROR: deterministic failure tail"
DEFAULT_TIME_WINDOW_HOURS = 24
FIXED_HISTORY_TASKS = 8

TASK_PARALLEL_SCALES = (5, 50, 200)
HISTORY_SCALES = (10, 50, 200)
LOG_SIZE_SCALES = (4 * 1024, 256 * 1024, 1024 * 1024)

REPO_ROOT = Path(__file__).resolve().parents[2]
BENCHMARK_ROOT = Path(__file__).resolve().parent
DEFAULT_OUTPUT_DIR = BENCHMARK_ROOT / "output"
DEFAULT_RESULTS_PATH = DEFAULT_OUTPUT_DIR / "results.json"
DEFAULT_REPORT_PATH = DEFAULT_OUTPUT_DIR / "report.md"
DEFAULT_MANIFEST_PATH = DEFAULT_OUTPUT_DIR / "seed_manifest.json"

TRACE_HEADER_NAME = "X-Metaflow-Trace-DB"
TRACE_HEADER_VALUE = "1"
DEFAULT_METADATA_POD_LABEL = "app.kubernetes.io/instance=metaflow-service"

METAFLOW_ROOT_ENV_VAR = "AGENTIC_BENCHMARK_METAFLOW_ROOT"
METADATA_POD_ENV_VAR = "AGENTIC_BENCHMARK_METADATA_POD"
DB_HOST_ENV_VAR = "AGENTIC_BENCHMARK_DB_HOST"
DB_PORT_ENV_VAR = "AGENTIC_BENCHMARK_DB_PORT"
DB_USER_ENV_VAR = "AGENTIC_BENCHMARK_DB_USER"
DB_PASSWORD_ENV_VAR = "AGENTIC_BENCHMARK_DB_PASSWORD"
DB_NAME_ENV_VAR = "AGENTIC_BENCHMARK_DB_NAME"

DEFAULT_DB_CONFIG = {
    "host": "localhost",
    "port": 5432,
    "user": "metaflow",
    "password": "metaflow123",
    "dbname": "metaflow",
}
